#resolving Ambiguous Method Names
puts " we need to be using composition wherever we see a 
case of ! uses a B or A has a B. our 
persisted person object won't subclass DataWrapper. 
instead it'll construct a reference to a database wrapper
object and use that object reference to save and restore 
itself"
